{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                "/var/deployment/packages/omnipay-paypal": "${workspaceFolder}"
            },
            "log": true,
            "xdebugSettings": {
                "max_data": 65536,
                "show_hidden": 1
            }
        }
    ]
}
